.unity-ui-document__root {
    flex-grow: 1;
    justify-content: center;
}

.unity-label {
    -unity-text-align: upper-center;
    font-size: 70px;
    -unity-font-style: normal;
    align-items: stretch;
}

.unity-button {
    flex-shrink: 0;
}
